diff --git a/crates/hclient/src/client.rs b/crates/hclient/src/client.rs
index f6486f2..626e82c 100644
--- a/crates/hclient/src/client.rs
+++ b/crates/hclient/src/client.rs
@@ -14,15 +14,18 @@ use hclient_core::{Capabilities, Error, ErrorKind, RequestBody, RetryKind, Unsup
 use hclient_proto::redirect::{RedirectAction, RedirectPolicy, decide};
 
 #[derive(Debug)]
-pub struct ClientBuilder<T, Tm = crate::DefaultClock> {
-    transport: T,
+pub struct ClientBuilder {
+    transport: Box<dyn hclient_core::unversioned::erased::BoxedTransport + Send + Sync>, // send-bound-exception: amendment-C12
+    /// The transport's type name, captured at construction: erasure loses
+    /// the type, and every capability refusal names the backend.
+    backend: &'static str,
     /// The clock a total timeout is measured with.
     ///
     /// Not an `Option`: the absence of a clock is a TYPE
     /// ([`crate::NoClock`]), not a `None`, so that no total timeout can be
     /// configured against a client that cannot measure one. See
     /// [`Self::total_timeout`] and [`crate::NoClock`]'s doc comment.
-    timer: Tm,
+    timer: std::sync::Arc<dyn hclient_core::unversioned::erased::BoxedTimer + Send + Sync>, // send-bound-exception: amendment-C12
     config: Config,
     /// The jar itself, on its way to `Inner`. `Config` carries only the
     /// bit that says one was asked for — see `Config::cookies` for why the
@@ -43,11 +46,14 @@ pub struct ClientBuilder<T, Tm = crate::DefaultClock> {
 /// and so uninferrable at the call site. Pinning it here is what keeps
 /// `Client::builder(t).build()` resolving to `Client<T>` — the bare form
 /// existing code writes — rather than to some other clock.
-impl<T: Transport> ClientBuilder<T, crate::DefaultClock> {
-    pub fn new(transport: T) -> Self {
+impl ClientBuilder {
+    pub fn new<T>(transport: T) -> Self
+    where
+        T: hclient_core::unversioned::erased::BoxedTransport + Send + Sync + 'static, // send-bound-exception: amendment-C12
+    {
         Self {
-            transport,
-            timer: crate::DefaultClock::default(),
+            transport: Box::new(transport),
+            timer: std::sync::Arc::new(crate::NoClock),
             config: Config::default(),
             #[cfg(feature = "cookies")]
             jar: None,
@@ -57,7 +63,7 @@ impl<T: Transport> ClientBuilder<T, crate::DefaultClock> {
     }
 }
 
-impl<T: Transport, Tm> ClientBuilder<T, Tm> {
+impl ClientBuilder {
     /// The redirect policy for every request from this client.
     ///
     /// Stores `Some(policy)`, and that wrapping carries meaning: it is what
@@ -294,7 +300,7 @@ impl<T: Transport, Tm> ClientBuilder<T, Tm> {
         self,
         timer: Tm2,
         total: Duration,
-    ) -> ClientBuilder<T, Tm2> {
+    ) -> ClientBuilder {
         ClientBuilder {
             transport: self.transport,
             timer,
@@ -455,11 +461,11 @@ impl<T: Transport, Tm> ClientBuilder<T, Tm> {
     /// Checks the configuration against the transport's capabilities. Not
     /// a single silent no-op: an unsupported setting is an error, here and
     /// now.
-    pub fn build(self) -> Result<Client<T, Tm>, UnsupportedCapability> {
+    pub fn build(self) -> Result<Client, UnsupportedCapability> {
         check_supported(
             &self.config,
             self.transport.capabilities(),
-            backend_name::<T>(),
+            self.backend,
         )?;
         Ok(Client {
             inner: std::sync::Arc::new(Inner {
@@ -505,43 +511,16 @@ fn backend_name<T>() -> &'static str {
 // `DefaultClock`'s `NoClock` branch in `lib.rs`, deliberately: `Client`
 // requires an explicit `T` exactly where there is no target-chosen clock
 // either.
-#[cfg(all(
-    feature = "default-transport",
-    not(all(target_family = "wasm", not(target_os = "unknown")))
-))]
-#[derive(Debug)]
-pub struct Client<T = crate::DefaultTransport, Tm = crate::DefaultClock> {
-    inner: std::sync::Arc<Inner<T, Tm>>,
-    config: Config,
-}
-#[cfg(any(
-    not(feature = "default-transport"),
-    all(target_family = "wasm", not(target_os = "unknown"))
-))]
-#[derive(Debug)]
-pub struct Client<T, Tm = crate::DefaultClock> {
-    inner: std::sync::Arc<Inner<T, Tm>>,
+#[derive(Debug, Clone)]
+pub struct Client {
+    inner: std::sync::Arc<Inner>,
     config: Config,
 }
 
-/// The transport and the clock a `Client` shares with its clones.
-///
-/// Behind an `Arc` so that cloning a `Client` is an atomic increment rather
-/// than a copy of the transport — which for a real backend owns a
-/// connection pool, a TLS configuration and a resolver, none of which
-/// should be duplicated by handing the client to a second task.
-///
-/// **The configuration is deliberately NOT in here**, unlike before v0.2
-/// W4. It sits in `Client` itself, per handle, and is cloned with it
-/// (`Timeouts` and `RedirectPolicy` are `Copy`; cloning an `http::Uri` is
-/// a `Bytes` refcount). That is what lets [`Client::total_timeout`] hand
-/// back a differently-bounded client over the SAME transport for the cost
-/// of one atomic increment — a per-call-site budget without a second
-/// connection pool.
-#[derive(Debug)]
-struct Inner<T, Tm> {
-    transport: T,
-    timer: Tm,
+struct Inner {
+    backend: &'static str,
+    transport: Box<dyn hclient_core::unversioned::erased::BoxedTransport + Send + Sync>, // send-bound-exception: amendment-C12
+    timer: std::sync::Arc<dyn hclient_core::unversioned::erased::BoxedTimer + Send + Sync>, // send-bound-exception: amendment-C12
     /// The cookie jar, if one was asked for — **here and not in `Config`**
     /// for the reason the `Config::cookies` bit records: a jar is shared
     /// state, and `Config` is cloned per handle.
@@ -572,21 +551,13 @@ struct Inner<T, Tm> {
 /// `T: Clone` and `Tm: Clone`, the first of which is both unnecessary —
 /// the `Arc` is what is cloned — and wrong in meaning, since a `T: Clone`
 /// transport would then be copied per clone instead of shared.
-impl<T, Tm> Clone for Client<T, Tm> {
-    fn clone(&self) -> Self {
-        Self {
-            inner: std::sync::Arc::clone(&self.inner),
-            config: self.config.clone(),
-        }
-    }
-}
 
 /// `Tm` fixed to [`crate::DefaultClock`] for the same reason
 /// `ClientBuilder::new` is — see its doc comment. `Client::builder(t)`
 /// takes only a transport, so nothing in the call could infer a clock;
 /// this is what keeps the result `Client<T>`, the bare form that already
 /// appears in this workspace's own signatures.
-impl<T: Transport> Client<T, crate::DefaultClock> {
+impl Client {
     pub fn builder(transport: T) -> ClientBuilder<T, crate::DefaultClock> {
         ClientBuilder::new(transport)
     }
@@ -613,7 +584,7 @@ impl<T: Transport> Client<T, crate::DefaultClock> {
 /// `#[cfg(feature = "default-transport")]`: without the feature
 /// `DefaultClock` *is* `NoClock`, so this method must not exist at all.
 #[cfg(feature = "default-transport")]
-impl<T: Transport> Client<T, crate::DefaultClock> {
+impl Client {
     /// A client sharing this one's transport, bounded by `total`.
     ///
     /// Consumes `self` and returns a new handle rather than mutating in
@@ -640,8 +611,8 @@ impl<T: Transport> Client<T, crate::DefaultClock> {
 /// it, `Clone` because the clock travels into every response body
 /// ([`crate::body::Deadline`] owns one). Both hold for every clock in this
 /// workspace, and for [`crate::NoClock`].
-impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
-    pub fn transport(&self) -> &T {
+impl Client {
+    pub fn transport(&self) -> &(dyn hclient_core::unversioned::erased::BoxedTransport + Send + Sync) { // send-bound-exception: amendment-C12
         &self.inner.transport
     }
     pub fn config(&self) -> &Config {
@@ -718,25 +689,25 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         Some(crate::cached::lock(self.inner.cache.as_ref()?))
     }
 
-    pub fn request(&self, method: http::Method, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn request(&self, method: http::Method, url: &str) -> RequestBuilder<'_> {
         RequestBuilder::new(self, method, url)
     }
-    pub fn get(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn get(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::GET, url)
     }
-    pub fn post(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn post(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::POST, url)
     }
-    pub fn put(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn put(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::PUT, url)
     }
-    pub fn delete(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn delete(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::DELETE, url)
     }
-    pub fn patch(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn patch(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::PATCH, url)
     }
-    pub fn head(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn head(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::HEAD, url)
     }
     /// HTTP QUERY: a **safe, idempotent** request that carries a body.
@@ -760,7 +731,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
     /// tests in `hclient-proto`, since the correct behaviour here follows
     /// only from QUERY not being POST, and would be easy to "fix" into
     /// corruption by anyone who groups it with POST for having a body.
-    pub fn query(&self, url: &str) -> RequestBuilder<'_, T, Tm> {
+    pub fn query(&self, url: &str) -> RequestBuilder<'_> {
         self.request(http::Method::QUERY, url)
     }
 
@@ -772,7 +743,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
     /// get a [`crate::sse::ReconnectingSseStream`] instead — that call is the
     /// actual gate between the two behaviors, not this method or any
     /// option on it.
-    pub fn sse(&self, url: &str) -> crate::sse::SseBuilder<'_, T, Tm> {
+    pub fn sse(&self, url: &str) -> crate::sse::SseBuilder<'_> {
         crate::sse::SseBuilder::new(self, url)
     }
 
@@ -795,7 +766,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
     pub async fn execute(
         &self,
         req: http::Request<RequestBody>,
-    ) -> Result<http::Response<crate::body::ClientBody<T::Body, Tm>>, Error>
+    ) -> Result<http::Response<crate::body::ClientBody>, Error>
     where
         T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     {
@@ -821,7 +792,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         #[cfg(feature = "digest-auth")] digest: Option<(String, String)>,
     ) -> Result<
         (
-            http::Response<crate::body::ClientBody<T::Body, Tm>>,
+            http::Response<crate::body::ClientBody>,
             http::Uri,
         ),
         Error,
@@ -932,7 +903,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         req: http::Request<RequestBody>,
         #[cfg(feature = "digest-auth")] mut digest: Option<(String, String)>,
-    ) -> Result<(http::Response<Cached<T::Body>>, http::Uri), Error>
+    ) -> Result<(http::Response<Cached<hclient_core::unversioned::erased::BoxBody>>, http::Uri), Error>
     where
         T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     {
@@ -974,7 +945,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         check_timeouts_supported(
             &effective,
             self.inner.transport.capabilities(),
-            backend_name::<T>(),
+            self.backend,
         )
         .map_err(|e| Error::new(ErrorKind::Unsupported, e))?;
         hp.extensions.insert(effective);
@@ -1004,7 +975,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         check_redirect_supported(
             &redirect,
             self.inner.transport.capabilities(),
-            backend_name::<T>(),
+            self.backend,
         )
         .map_err(|e| Error::new(ErrorKind::Unsupported, e))?;
         let redirect = redirect.unwrap_or_default();
@@ -1031,7 +1002,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         check_version_demand_supported(
             &hp.extensions,
             self.inner.transport.capabilities(),
-            backend_name::<T>(),
+            self.backend,
         )
         .map_err(|e| Error::new(ErrorKind::Unsupported, e))?;
 
@@ -1374,7 +1345,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         hp: &HopParts,
         body: RequestBody,
-    ) -> Result<http::Response<T::Body>, Error>
+    ) -> Result<http::Response<hclient_core::unversioned::erased::BoxBody>, Error>
     where
         T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     {
@@ -1528,7 +1499,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         hp: &mut HopParts,
         caller_owns_the_conditionals: bool,
-    ) -> std::ops::ControlFlow<http::Response<Cached<T::Body>>, Plan> {
+    ) -> std::ops::ControlFlow<http::Response<Cached<hclient_core::unversioned::erased::BoxBody>>, Plan> {
         use std::ops::ControlFlow::{Break, Continue};
         let Some(cache) = self.inner.cache.as_ref() else {
             return Continue(Plan::default());
@@ -1570,7 +1541,7 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         _: &mut HopParts,
         _: bool,
-    ) -> std::ops::ControlFlow<http::Response<Cached<T::Body>>, Plan> {
+    ) -> std::ops::ControlFlow<http::Response<Cached<hclient_core::unversioned::erased::BoxBody>>, Plan> {
         std::ops::ControlFlow::Continue(Plan)
     }
 
@@ -1600,9 +1571,9 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         hp: &HopParts,
         plan: Plan,
-        resp: http::Response<T::Body>,
+        resp: http::Response<hclient_core::unversioned::erased::BoxBody>,
         requested_at: std::time::SystemTime,
-    ) -> http::Response<Cached<T::Body>> {
+    ) -> http::Response<Cached<hclient_core::unversioned::erased::BoxBody>> {
         let Some(cache) = self.inner.cache.as_ref() else {
             return resp.map(Cached::live);
         };
@@ -1659,9 +1630,9 @@ impl<T: Transport, Tm: Timer + Clone> Client<T, Tm> {
         &self,
         _: &HopParts,
         _: Plan,
-        resp: http::Response<T::Body>,
+        resp: http::Response<hclient_core::unversioned::erased::BoxBody>,
         _: std::time::SystemTime,
-    ) -> http::Response<Cached<T::Body>> {
+    ) -> http::Response<Cached<hclient_core::unversioned::erased::BoxBody>> {
         resp.map(Cached::live)
     }
 }
@@ -1713,7 +1684,7 @@ fn lock<T>(m: &std::sync::Mutex<T>) -> std::sync::MutexGuard<'_, T> {
 // `impl` for a nonexistent type also fails to compile), but repeating the
 // condition makes the reason visible on the spot, not only in `lib.rs`.
 #[cfg(all(feature = "default-transport", not(target_family = "wasm")))]
-impl Client<crate::DefaultTransport> {
+impl Client {
     /// A client with the default transport.
     ///
     /// On native this requires a surrounding tokio runtime: `tokio::spawn`
@@ -1808,7 +1779,7 @@ impl Client<crate::DefaultTransport> {
     target_family = "wasm",
     target_os = "unknown"
 ))]
-impl Client<crate::DefaultTransport> {
+impl Client {
     /// A client with the browser transport.
     ///
     /// No `Result`, unlike the native [`Client::new`] — and no panic
@@ -1861,7 +1832,7 @@ impl Client<crate::DefaultTransport> {
     target_family = "wasm",
     target_os = "unknown"
 ))]
-impl Default for Client<crate::DefaultTransport> {
+impl Default for Client {
     fn default() -> Self {
         Self::new()
     }
diff --git a/crates/hclient/src/deadline.rs b/crates/hclient/src/deadline.rs
index 8e6f706..7fcea55 100644
--- a/crates/hclient/src/deadline.rs
+++ b/crates/hclient/src/deadline.rs
@@ -207,12 +207,12 @@ where
 /// ambient exchange behind it is torn down rather than left draining. A
 /// bound that returned an error and left the transfer running would be a
 /// bound on the caller's patience, not on the operation.
-pub struct Deadline<B, Tm: Timer> {
+pub struct Deadline<B> {
     /// `None` once the deadline has fired: the body is dropped there, and
     /// dropping it is what stops the exchange.
     inner: Option<B>,
-    timer: Tm,
-    started: Tm::Instant,
+    timer: std::sync::Arc<dyn hclient_core::unversioned::erased::BoxedTimer + Send + Sync>, // send-bound-exception: amendment-C12
+    started: hclient_core::unversioned::erased::BoxInstant,
     /// `None` — no bound was set, and this wrapper is inert. It is still
     /// in the type, because a type cannot appear and disappear with a
     /// runtime value; the cost is one `Option` test per frame.
@@ -228,7 +228,7 @@ pub struct Deadline<B, Tm: Timer> {
     sleep: Option<Pin<Box<Tm::Sleep>>>,
 }
 
-impl<B, Tm: Timer> Deadline<B, Tm> {
+impl<B> Deadline<B> {
     /// Builds the wrapper, and with it the sleep that will cut a silent
     /// body.
     ///
@@ -319,12 +319,12 @@ impl<B, Tm: Timer> Deadline<B, Tm> {
 /// here because nothing in this type is ever pinned in place: the only
 /// projection is `Pin::new(&mut inner)`, which needs `B: Unpin` on its
 /// own, and no `unsafe` appears in this crate at all (`#![forbid]`).
-impl<B: Unpin, Tm: Timer> Unpin for Deadline<B, Tm> {}
+impl<B: Unpin, Tm: Timer> Unpin for Deadline<B> {}
 
 /// Hand-written: `#[derive(Debug)]` would require `Tm::Instant: Debug`,
 /// which [`Timer`] does not ask for, so the derive would not compile for a
 /// clock whose instant is not `Debug`. The instant is not printed.
-impl<B: std::fmt::Debug, Tm: Timer> std::fmt::Debug for Deadline<B, Tm> {
+impl<B: std::fmt::Debug, Tm: Timer> std::fmt::Debug for Deadline<B> {
     fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
         f.debug_struct("Deadline")
             .field("inner", &self.inner)
@@ -333,7 +333,7 @@ impl<B: std::fmt::Debug, Tm: Timer> std::fmt::Debug for Deadline<B, Tm> {
     }
 }
 
-impl<B, Tm> http_body::Body for Deadline<B, Tm>
+impl<B> http_body::Body for Deadline<B>
 where
     B: http_body::Body<Data = Bytes> + Unpin,
     // The same `send-bound-exception: amendment-C1` point `Response::chunk`
diff --git a/crates/hclient/src/lib.rs b/crates/hclient/src/lib.rs
index e9a0238..249463d 100644
--- a/crates/hclient/src/lib.rs
+++ b/crates/hclient/src/lib.rs
@@ -156,7 +156,7 @@ pub mod body {
     /// enum test per frame for that — and without the `cache` feature
     /// [`Cached`] is a newtype over `Option<B>` whose other two fields do not
     /// exist, so the third wrapper costs nothing a build did not ask for.
-    pub type ClientBody<B, Tm> = Limited<Decompressed<Deadline<Cached<B>, Tm>>>;
+    pub type ClientBody = Limited<Decompressed<Deadline<Cached<hclient_core::unversioned::erased::BoxBody>>>>;
 }
 
 /// Following redirects: how many, and whether this one.
diff --git a/crates/hclient/src/request.rs b/crates/hclient/src/request.rs
index 3632c89..1dc2c70 100644
--- a/crates/hclient/src/request.rs
+++ b/crates/hclient/src/request.rs
@@ -10,8 +10,8 @@ use hclient_proto::redirect::RedirectPolicy;
 /// same default as `Client`'s own parameter, so `RequestBuilder<'_, T>`
 /// keeps naming what it always named.
 #[derive(Debug)]
-pub struct RequestBuilder<'a, T, Tm = crate::DefaultClock> {
-    client: &'a Client<T, Tm>,
+pub struct RequestBuilder<'a> {
+    client: &'a Client,
     method: http::Method,
     /// Already resolved against the client's `base_url` (or just parsed,
     /// if there's no base). Resolution lives in `new`, not in `send`,
@@ -50,8 +50,8 @@ pub struct RequestBuilder<'a, T, Tm = crate::DefaultClock> {
     error: Option<Error>,
 }
 
-impl<'a, T: Transport, Tm: Timer + Clone> RequestBuilder<'a, T, Tm> {
-    pub(crate) fn new(client: &'a Client<T, Tm>, method: http::Method, url: &str) -> Self {
+impl<'a> RequestBuilder<'a> {
+    pub(crate) fn new(client: &'a Client, method: http::Method, url: &str) -> Self {
         Self {
             client,
             method,
@@ -433,7 +433,7 @@ impl<'a, T: Transport, Tm: Timer + Clone> RequestBuilder<'a, T, Tm> {
     /// client's whole-operation bound past the response head — inert, and
     /// costing one `Option` test per frame, for a client that never set
     /// one.
-    pub async fn send(self) -> Result<Response<crate::body::ClientBody<T::Body, Tm>>, Error>
+    pub async fn send(self) -> Result<Response<crate::body::ClientBody>, Error>
     where
         // Sibling of the bound on `Client::execute` in `client.rs` (spec
         // amendment-C1): `send` calls `Client::execute`, which requires
diff --git a/crates/hclient/src/response.rs b/crates/hclient/src/response.rs
index 2c1faba..710e052 100644
--- a/crates/hclient/src/response.rs
+++ b/crates/hclient/src/response.rs
@@ -127,7 +127,7 @@ where
     // stores its source as `Arc<dyn Error + Send + Sync>`, and type erasure
     // doesn't let an unbounded trait object's auto-traits through. The same
     // bound as `Client::execute`, only this time required on
-    // `T::Body::Error` rather than `T::Error` — the body is read after the
+    // `hclient_core::unversioned::erased::BoxBody::Error` rather than `T::Error` — the body is read after the
     // transport has already returned it.
     B::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
 {
diff --git a/crates/hclient/src/sse.rs b/crates/hclient/src/sse.rs
index 63bd9f5..35739fd 100644
--- a/crates/hclient/src/sse.rs
+++ b/crates/hclient/src/sse.rs
@@ -275,7 +275,7 @@ impl Default for SseOptions {
 /// independent on purpose: a caller can want reconnect without a total
 /// bound, or a bound without reconnect.
 #[derive(Debug)]
-pub struct SseBuilder<'a, T, C = crate::DefaultClock> {
+pub struct SseBuilder<'a> {
     client: &'a Client<T, C>,
     url: String,
     headers: http::HeaderMap,
@@ -289,7 +289,7 @@ pub struct SseBuilder<'a, T, C = crate::DefaultClock> {
     error: Option<Error>,
 }
 
-impl<'a, T: Transport, C: Timer + Clone> SseBuilder<'a, T, C> {
+impl<'a, T: Transport, C: Timer + Clone> SseBuilder<'a> {
     pub(crate) fn new(client: &'a Client<T, C>, url: &str) -> Self {
         Self {
             client,
@@ -374,7 +374,7 @@ impl<'a, T: Transport, C: Timer + Clone> SseBuilder<'a, T, C> {
     /// `Duration` and resolves immediately, so a test can assert the
     /// backoff actually computed the interval it should have, which a
     /// thread-backed clock could only do by really waiting.
-    pub fn with_timer<Tm: Timer>(self, timer: Tm) -> ReconnectingSseBuilder<'a, T, C, Tm> {
+    pub fn with_timer<Tm: Timer>(self, timer: Tm) -> ReconnectingSseBuilder<'a> {
         ReconnectingSseBuilder {
             builder: self,
             timer,
@@ -384,10 +384,10 @@ impl<'a, T: Transport, C: Timer + Clone> SseBuilder<'a, T, C> {
     /// A single connection attempt, exactly [`SseStream::new`]'s contract —
     /// no reconnect, because there is no timer to wait out a backoff delay
     /// with. For reconnect, add [`with_timer`](Self::with_timer) first.
-    pub async fn connect(self) -> Result<SseStream<crate::body::ClientBody<T::Body, C>>, Error>
+    pub async fn connect(self) -> Result<SseStream<crate::body::ClientBody<hclient_core::unversioned::erased::BoxBody, C>>, Error>
     where
-        T::Body: HttpBody<Data = Bytes> + Unpin,
-        <T::Body as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
+        hclient_core::unversioned::erased::BoxBody: HttpBody<Data = Bytes> + Unpin,
+        <hclient_core::unversioned::erased::BoxBody as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
         T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     {
         if let Some(e) = self.error {
@@ -406,12 +406,12 @@ impl<'a, T: Transport, C: Timer + Clone> SseBuilder<'a, T, C> {
 
 /// Builds a [`ReconnectingSseStream`]. Returned by [`SseBuilder::with_timer`].
 #[derive(Debug)]
-pub struct ReconnectingSseBuilder<'a, T, C, Tm> {
-    builder: SseBuilder<'a, T, C>,
+pub struct ReconnectingSseBuilder<'a> {
+    builder: SseBuilder<'a>,
     timer: Tm,
 }
 
-impl<'a, T: Transport, C: Timer + Clone, Tm: Timer> ReconnectingSseBuilder<'a, T, C, Tm> {
+impl<'a, T: Transport, C: Timer + Clone, Tm: Timer> ReconnectingSseBuilder<'a> {
     /// Forwards to [`SseBuilder::header`] — see its doc comment.
     pub fn header(mut self, name: &str, value: &str) -> Self {
         self.builder = self.builder.header(name, value);
@@ -433,10 +433,10 @@ impl<'a, T: Transport, C: Timer + Clone, Tm: Timer> ReconnectingSseBuilder<'a, T
     /// started yet. Reconnect (with backoff) only applies to a stream that
     /// was successfully opened at least once and later dropped — see
     /// `ReconnectingSseStream::next`.
-    pub async fn connect(self) -> Result<ReconnectingSseStream<'a, T, C, Tm>, Error>
+    pub async fn connect(self) -> Result<ReconnectingSseStream<'a>, Error>
     where
-        T::Body: HttpBody<Data = Bytes> + Unpin,
-        <T::Body as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
+        hclient_core::unversioned::erased::BoxBody: HttpBody<Data = Bytes> + Unpin,
+        <hclient_core::unversioned::erased::BoxBody as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
         T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     {
         if let Some(e) = self.builder.error {
@@ -478,12 +478,12 @@ async fn open<T, C>(
     url: &str,
     last_event_id: Option<&str>,
     max_event_size: usize,
-) -> Result<SseStream<crate::body::ClientBody<T::Body, C>>, Error>
+) -> Result<SseStream<crate::body::ClientBody<hclient_core::unversioned::erased::BoxBody, C>>, Error>
 where
     T: Transport,
     C: Timer + Clone,
-    T::Body: HttpBody<Data = Bytes> + Unpin,
-    <T::Body as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
+    hclient_core::unversioned::erased::BoxBody: HttpBody<Data = Bytes> + Unpin,
+    <hclient_core::unversioned::erased::BoxBody as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
     T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
 {
     let uri = crate::config::effective_uri(client.config().base_url.as_ref(), url)?;
@@ -649,7 +649,7 @@ struct ReconnectExhausted {
     /// the configured policy.
     attempts: u32,
 }
-/// Where a [`ReconnectingSseStream`] currently stands. `B` is `T::Body` —
+/// Where a [`ReconnectingSseStream`] currently stands. `B` is `hclient_core::unversioned::erased::BoxBody` —
 /// the SAME concrete body type every (re)connection produces, since every
 /// (re)connection goes through the same `Client<T>`.
 #[derive(Debug)]
@@ -677,7 +677,7 @@ enum ReconnectState<B> {
 /// `Tm`. Built by [`ReconnectingSseBuilder::connect`], reachable from
 /// [`Client::sse`]`.with_timer(..)`.
 #[derive(Debug)]
-pub struct ReconnectingSseStream<'a, T: Transport, C: Timer, Tm> {
+pub struct ReconnectingSseStream<'a> {
     client: &'a Client<T, C>,
     url: String,
     headers: http::HeaderMap,
@@ -706,14 +706,14 @@ pub struct ReconnectingSseStream<'a, T: Transport, C: Timer, Tm> {
     /// The most recently seen server `retry:` value, if any — see `next`'s
     /// doc comment for how it interacts with `options.backoff`.
     server_retry: Option<Duration>,
-    state: ReconnectState<crate::body::ClientBody<T::Body, C>>,
+    state: ReconnectState<crate::body::ClientBody<hclient_core::unversioned::erased::BoxBody, C>>,
 }
 
-impl<'a, T, C, Tm> ReconnectingSseStream<'a, T, C, Tm>
+impl<'a, T, C, Tm> ReconnectingSseStream<'a>
 where
     T: Transport,
-    T::Body: HttpBody<Data = Bytes> + Unpin,
-    <T::Body as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
+    hclient_core::unversioned::erased::BoxBody: HttpBody<Data = Bytes> + Unpin,
+    <hclient_core::unversioned::erased::BoxBody as HttpBody>::Error: std::error::Error + Send + Sync + 'static, // send-bound-exception: amendment-C1
     T::Error: Send + Sync + 'static, // send-bound-exception: amendment-C1
     C: Timer + Clone,
     Tm: Timer,
